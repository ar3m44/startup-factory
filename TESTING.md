# 🧪 Testing Guide - Factory OS

Инструкции по тестированию автономной системы.

## Quick Start

1. **Запустить dev сервер:**
   ```bash
   npm run dev
   ```

2. **Открыть Control Panel:**
   - Откройте http://localhost:3000/factory в браузере
   - Вы должны увидеть терминальный интерфейс с зелёным текстом

## Тест 1: Просмотр State

**Что тестируем:** API endpoint `/api/state` и отображение данных в UI

**Шаги:**
1. Откройте http://localhost:3000/factory
2. Проверьте Dashboard статистику:
   - VENTURES: 0
   - SIGNALS: 1 (наш тестовый сигнал)
   - MRR: 0₽
   - BUDGET: 0₽ / 50,000₽

**Ожидаемый результат:**
- ✅ Страница загружается без ошибок
- ✅ Виден 1 сигнал в списке SIGNALS
- ✅ ID сигнала: SIGNAL-2025-01-17-12-00
- ✅ Source: Reddit
- ✅ Confidence: 85/100
- ✅ Status: pending_validation
- ✅ Кнопка "VALIDATE" активна

## Тест 2: Валидация Сигнала

**Что тестируем:** API endpoint `/api/validator` и работу Validator агента

**Шаги:**
1. На странице /factory найдите сигнал SIGNAL-2025-01-17-12-00
2. Нажмите кнопку "▶ VALIDATE"
3. Дождитесь окончания (может занять 5-10 секунд)

**Ожидаемый результат:**
- ✅ Показан alert с результатами:
  - Решение: GO или NO-GO
  - Статусы 5 пайплайнов (TAM, Competitors, Technical, Pricing, Risks)
  - Если GO → показаны детали Venture Blueprint
- ✅ Статус сигнала изменился на "validated" или "rejected"
- ✅ Появилась строка "Validation: VALIDATION-2025-01-17-..."

## Тест 3: Проверка Сохранённых Файлов

**Что тестируем:** Сохранение результатов валидации в файлы

**Шаги:**
1. После валидации откройте директорию `factory/pipelines/`
2. Должен появиться новый файл `VALIDATION-2025-01-17-....md`

**Ожидаемый результат:**
- ✅ Файл существует
- ✅ Содержит markdown отчёт с:
  - Signal ID
  - Decision (GO/NO-GO)
  - Таблица с результатами 5 пайплайнов
  - Детальный анализ каждого пайплайна
  - Venture Blueprint (если GO)

## Тест 4: Обновление State

**Что тестируем:** Обновление state.json после валидации

**Шаги:**
1. Откройте `factory/state.json`
2. Найдите сигнал с ID "SIGNAL-2025-01-17-12-00"

**Ожидаемый результат:**
- ✅ `status` изменился на "validated" или "rejected"
- ✅ Поле `validationId` заполнено (например: "VALIDATION-2025-01-17-...")
- ✅ `lastValidatorRun` обновлён на текущую дату

## Тест 5: Run Scout (Mock)

**Что тестируем:** API endpoint `/api/scout`

**Шаги:**
1. На странице /factory нажмите кнопку "▶ RUN Scout Agent"
2. Дождитесь окончания

**Ожидаемый результат:**
- ✅ Показан alert: "Scout найден 0 сигналов" (т.к. Scout пока возвращает mock данные)
- ✅ Кнопка вернулась в нормальное состояние
- ✅ В секции ACTIONS обновилась строка "Last run: ..."

## Тест 6: API Endpoints Напрямую

**Что тестируем:** Работу API без UI

**GET /api/state:**
```bash
curl http://localhost:3000/api/state | jq .
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "state": {
    "ventures": [],
    "signals": [...],
    "budget": { "monthly": 50000, "spent": 0 },
    "stats": { "totalVentures": 0, ... }
  }
}
```

**POST /api/validator:**
```bash
curl -X POST http://localhost:3000/api/validator \
  -H "Content-Type: application/json" \
  -d '{"signalId":"SIGNAL-2025-01-17-12-00"}' | jq .
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "validation": {
    "id": "VALIDATION-...",
    "decision": "GO" | "NO-GO",
    "pipelines": { "tam": "GREEN", ... }
  }
}
```

## Тест 7: Создание Venture (если GO)

**Что тестируем:** Создание venture при GO решении

**Шаги:**
1. Если валидация вернула GO, проверьте директорию `ventures/`
2. Должна появиться папка `V-2025-001-...`

**Ожидаемый результат:**
- ✅ Директория создана
- ✅ Файл `README.md` содержит детали venture
- ✅ Файл `RISKS.md` содержит список рисков
- ✅ В `state.json` появился новый venture

## Проблемы и Решения

### Ошибка: "Cannot find module '@/lib/orchestrator'"
**Решение:** Убедитесь что запустили `npm run dev`, а не прямой node

### Ошибка: "State file not found"
**Решение:** Нормально, система создаёт начальный state автоматически

### Сигналы не отображаются в UI
**Решение:** Проверьте что в `factory/state.json` есть массив signals

### Валидация висит бесконечно
**Решение:** Откройте DevTools (F12) → Console и проверьте ошибки

## Следующие Шаги

После успешного тестирования:
1. ✅ Все тесты пройдены → система работает!
2. 🚀 Phase 2: Добавить реальный Scout (Reddit/HN API)
3. 🤖 Phase 3: GitHub Actions automation (cron)

## Test Checklist

- [ ] Тест 1: State API загружается
- [ ] Тест 2: Валидация сигнала работает
- [ ] Тест 3: Файл валидации сохранён
- [ ] Тест 4: State обновлён
- [ ] Тест 5: Run Scout вызывается
- [ ] Тест 6: API endpoints работают напрямую
- [ ] Тест 7: Venture создаётся (если GO)

---

**Готово к production:** После прохождения всех тестов система готова к деплою на Vercel!
