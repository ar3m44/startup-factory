# Continue.dev Integration - Local Code Generation

Используйте Continue.dev для локальной генерации кода ventures прямо в VS Code.

## 🎯 Преимущества

- ✅ **Локальная генерация** - видите результат сразу в редакторе
- ✅ **Интерактивность** - можете корректировать генерацию в процессе
- ✅ **Гибкость** - редактируйте код сразу после генерации
- ✅ **Контроль** - полный контроль над процессом
- ✅ **Дешевле** - используете свой OpenAI API ключ напрямую

---

## 📦 Установка Continue.dev

### Шаг 1: Установить расширение

1. Откройте VS Code
2. Нажмите `Cmd+Shift+X` (или `Ctrl+Shift+X` на Windows)
3. Найдите **"Continue"**
4. Нажмите **"Install"**

Или установите из командной строки:
```bash
code --install-extension continue.continue
```

### Шаг 2: Настроить API ключ

1. После установки откройте Continue sidebar:
   - `Cmd+L` (Mac) или `Ctrl+L` (Windows/Linux)

2. Кликните на **⚙️ Settings** (шестеренка внизу)

3. В файле `config.json` найдите:
   ```json
   "models": [
     {
       "title": "GPT-4 Turbo",
       "provider": "openai",
       "apiKey": "",  // ← Вставьте ваш ключ сюда
       ...
     }
   ]
   ```

4. Вставьте ваш OpenAI API ключ:
   ```json
   "apiKey": "sk-proj-ваш-ключ-здесь"
   ```

5. Сохраните файл (`Cmd+S` или `Ctrl+S`)

### Шаг 3: Скопировать конфигурацию Factory OS

Factory OS уже создал оптимизированную конфигурацию:

```bash
# Скопируйте конфиг в Continue
cp .continue/config.json ~/.continue/config.json
```

Затем откройте `~/.continue/config.json` и добавьте ваш `OPENAI_API_KEY`.

---

## 🚀 Использование

### Вариант 1: Автоматический запуск (Рекомендуется)

Запустите скрипт, который все сделает за вас:

```bash
tsx scripts/codex-local.ts \
  --ventureId V-2026-001-typescript-1 \
  --taskFile factory/tasks/V-2026-001-typescript-1.md
```

Скрипт:
1. ✅ Создаст директорию `ventures/V-2026-001-typescript-1/`
2. ✅ Создаст файл инструкций для Continue
3. ✅ Откроет VS Code в этой директории
4. ✅ Покажет инструкции, что делать дальше

### Вариант 2: Ручной запуск

1. **Создайте директорию venture:**
   ```bash
   mkdir -p ventures/V-2026-001-typescript-1
   cd ventures/V-2026-001-typescript-1
   ```

2. **Откройте VS Code:**
   ```bash
   code .
   ```

3. **Откройте Continue sidebar:**
   - Нажмите `Cmd+L` (Mac) или `Ctrl+L` (Windows)

4. **Загрузите task file:**
   - В Continue chat введите:
     ```
     @factory/tasks/V-2026-001-typescript-1.md
     ```

5. **Запустите генерацию:**
   - Введите в Continue chat:
     ```
     /venture
     ```
   - Или просто:
     ```
     Generate complete Next.js venture following the task file
     ```

6. **Continue сгенерирует код:**
   - Будут созданы все файлы
   - Вы увидите их в VS Code
   - Можете сразу редактировать

---

## 💬 Custom Commands в Continue

Factory OS добавил специальные команды:

### `/venture` - Генерация venture
Генерирует полный проект из task file.

**Использование:**
```
@factory/tasks/V-2026-001-typescript-1.md

/venture
```

### `/fix` - Исправление ошибок
Исправляет TypeScript/ESLint ошибки и улучшает код.

**Использование:**
```
@src/app/page.tsx

/fix
```

### `/feature` - Добавление фичи
Добавляет новую фичу в существующий код.

**Использование:**
```
@src/app/page.tsx

/feature Add a dark mode toggle
```

---

## 🎨 Workflow генерации

### 1. Запуск генерации

```bash
# Из корня проекта
tsx scripts/codex-local.ts \
  --ventureId V-2026-001-typescript-1 \
  --taskFile factory/tasks/V-2026-001-typescript-1.md
```

### 2. В VS Code

Continue откроется автоматически. Вы увидите:
- 📂 Директория `ventures/V-2026-001-typescript-1/`
- 📄 Файл `VENTURE_INSTRUCTIONS.md` с заданием

### 3. Генерация кода

В Continue chat:
```
@VENTURE_INSTRUCTIONS.md

Generate all files for this venture
```

Continue начнет генерировать:
```
Creating package.json...
Creating src/app/layout.tsx...
Creating src/app/page.tsx...
Creating src/components/UI/Button.tsx...
...
```

### 4. Review и редактирование

- ✅ Файлы создаются прямо в VS Code
- ✅ Можете останавливать генерацию
- ✅ Можете редактировать на лету
- ✅ Можете попросить Continue исправить что-то

### 5. Тестирование

```bash
cd ventures/V-2026-001-typescript-1
npm install
npm run dev
```

Откройте http://localhost:3000

### 6. Commit и Push

```bash
git add ventures/V-2026-001-typescript-1
git commit -m "Add V-2026-001-typescript-1 venture"
git push origin main
```

---

## 🎯 Примеры использования

### Пример 1: Генерация с нуля

```bash
# 1. Запуск
tsx scripts/codex-local.ts \
  --ventureId V-2026-001-typescript-1 \
  --taskFile factory/tasks/V-2026-001-typescript-1.md

# 2. В VS Code Continue:
@VENTURE_INSTRUCTIONS.md

Generate complete Next.js venture with all features from the task

# 3. Continue генерирует ~20-30 файлов
# 4. Review и test
# 5. Commit
```

### Пример 2: Исправление ошибок

```bash
# В Continue chat:
@src/app/page.tsx
@src/components/Features/Parser.tsx

/fix

Fix all TypeScript errors and improve code quality
```

### Пример 3: Добавление фичи

```bash
# В Continue chat:
@src/app/page.tsx

/feature Add email subscription form in footer
```

---

## ⚙️ Конфигурация

### Изменить модель

Откройте `~/.continue/config.json`:

```json
"models": [
  {
    "title": "GPT-4 Turbo",  // <- Используется по умолчанию
    "model": "gpt-4-turbo-preview"
  },
  {
    "title": "GPT-3.5 Turbo",  // <- Дешевле и быстрее
    "model": "gpt-3.5-turbo"
  }
]
```

Переключиться на GPT-3.5:
1. В Continue sidebar кликните на текущую модель
2. Выберите "GPT-3.5 Turbo"

### Добавить свои команды

В `~/.continue/config.json`:

```json
"customCommands": [
  {
    "name": "your-command",
    "description": "Your custom command",
    "prompt": "Your prompt here..."
  }
]
```

---

## 🆚 Сравнение: Continue vs GitHub Actions

| Параметр | Continue.dev (Local) | GitHub Actions (Remote) |
|----------|---------------------|------------------------|
| **Скорость** | ⚡ Мгновенно | 🐌 2-5 минут setup |
| **Интерактивность** | ✅ Полная | ❌ Нет |
| **Редактирование** | ✅ Сразу | ❌ Только после PR |
| **Стоимость** | 💰 ~$0.20-0.40 | 💰 ~$0.30-0.50 |
| **Контроль** | ✅ Полный | ⚠️ Частичный |
| **Автоматизация** | ⚠️ Полу-автомат | ✅ Полный автомат |
| **Review** | ✅ В процессе | ⚠️ После генерации |

**Рекомендация**:
- 🏗️ **Разработка** → Continue.dev (быстро, интерактивно)
- 🤖 **Автоматизация** → GitHub Actions (полностью автономно)

---

## 💡 Tips & Tricks

### 1. Используйте контекст

Добавляйте файлы в контекст через `@`:
```
@src/app/page.tsx
@src/components/UI/Button.tsx

Update Button to match the page style
```

### 2. Итеративная генерация

Генерируйте постепенно:
```
1. Generate package.json and basic structure
2. Generate landing page
3. Generate API routes
4. Generate components
```

### 3. Используйте diff

Continue показывает diff перед применением изменений:
- ✅ Принять все изменения
- ❌ Отклонить
- ✏️ Редактировать вручную

### 4. Кастомные промпты

Сохраняйте промпты в `.continue/prompts/`:
```bash
mkdir -p .continue/prompts
echo "Your custom prompt" > .continue/prompts/my-prompt.md
```

Затем:
```
@.continue/prompts/my-prompt.md
```

---

## 🐛 Troubleshooting

### Continue не видит конфиг
```bash
# Убедитесь, что файл в правильном месте
ls ~/.continue/config.json

# Если нет, скопируйте
cp .continue/config.json ~/.continue/config.json
```

### API ключ не работает
1. Проверьте формат: должен начинаться с `sk-proj-...` или `sk-...`
2. Проверьте баланс: https://platform.openai.com/usage
3. Перезапустите VS Code

### Генерация зависла
- Нажмите кнопку Stop в Continue
- Попробуйте с меньшим контекстом
- Переключитесь на GPT-3.5 для быстрой генерации

### Слишком дорого
- Используйте GPT-3.5 вместо GPT-4
- Генерируйте по частям
- Используйте GitHub Actions (фиксированная стоимость)

---

## 📚 Дополнительные ресурсы

- Continue.dev Docs: https://docs.continue.dev
- OpenAI API Docs: https://platform.openai.com/docs
- Factory OS Prompts: `scripts/prompts/`
- Venture Templates: `scripts/templates/`

---

**Continue.dev готов к использованию!** 🚀

Попробуйте сгенерировать первый venture локально:

```bash
tsx scripts/codex-local.ts \
  --ventureId V-2026-001-typescript-1 \
  --taskFile factory/tasks/V-2026-001-typescript-1.md
```
